{% extends '::base.html.twig' %}

{% block body -%}
    <h1>LigneHorsForfait</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Datejour</th>
                <td>{{ entity.dateJour }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ entity.montant }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entity.libelle }}</td>
            </tr>
            <tr>
                <th>Motifrefus</th>
                <td>{{ entity.motifRefus }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('lignehorsforfait') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('lignehorsforfait_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
