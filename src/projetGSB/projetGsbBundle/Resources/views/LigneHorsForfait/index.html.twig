{% extends '::base.html.twig' %}

{% block body -%}
    <h1>LigneHorsForfait list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Datejour</th>
                <th>Montant</th>
                <th>Libelle</th>
                <th>Motifrefus</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('lignehorsforfait_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.dateJour }}</td>
                <td>{{ entity.montant }}</td>
                <td>{{ entity.libelle }}</td>
                <td>{{ entity.motifRefus }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('lignehorsforfait_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('lignehorsforfait_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('lignehorsforfait_new') }}">
                Create a new entry
            </a>
        </li>
        <li>
            <a href="{{ path('projetGSB_admin_index') }}">
                Return to home page
            </a>
        </li>
    </ul>
    {% endblock %}
