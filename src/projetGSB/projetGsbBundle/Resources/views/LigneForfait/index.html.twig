{% extends '::base.html.twig' %}

{% block body -%}
    <h1>LigneForfait list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Repasmidi</th>
                <th>Nuitees</th>
                <th>Etape</th>
                <th>Km</th>
                <th>Datejourdebut</th>
                <th>Datejourfin</th>
                <th>Motifrefus</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ligneforfait_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.repasMidi }}</td>
                <td>{{ entity.nuitees }}</td>
                <td>{{ entity.etape }}</td>
                <td>{{ entity.km }}</td>
                <td>{{ entity.dateJourDebut }}</td>
                <td>{{ entity.dateJourFin }}</td>
                <td>{{ entity.motifRefus }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ligneforfait_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ligneforfait_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ligneforfait_new') }}">
                Create a new entry
            </a>
        </li>
        <li>
            <a href="{{ path('projetGSB_admin_index') }}">
                Return to home page
            </a>
        </li>
    </ul>
    {% endblock %}
